# Toy Greeter

A tiny Kotlin example: a single-file program that prints `hello` and a short
note about the project. Two source files and one notes file. Used by the
kt-coding-agent demo to exercise `list_files` and `read_file`.
